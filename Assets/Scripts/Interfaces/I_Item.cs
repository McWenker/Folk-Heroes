using UnityEngine;
public interface I_Item
{
    /*void PickUp();
    void Drop();
    void Remove();
    void BeginMouse(Transform user, Vector3 targetPoint, Transform handSpot, int useType);
    void HoldMouse(Transform user, Vector3 targetPoint, Transform handSpot, int useType);
    void EndMouse(Transform user, Vector3 targetPoint, Transform handSpot, int useType);
    void Use(Transform user, Vector3 targetPoint, Transform handSpot, int useType, float holdFactor);
    void Trigger(Object sender, ItemUses use);*/
}
